A lightweight Python-based web crawler designed to discover all domain‑specific URLs within a target website.
It provides flexible controls that allow users to define the crawl depth from scanning the entire site to limiting traversal based on project needs.
Users can also specify the maximum number of links to collect, choosing either to retrieve every available URL or restrict discovery to a defined limit.
